{"disposables": [
  {"path": ".venv", "category": "venv",
    "reason": "recreatable in ~1 min from pyproject.toml via `uv venv .venv && uv pip install -e .`"},
  {"path": "__pycache__", "category": "build",
    "reason": "Python bytecode cache, regenerated automatically on import"},
  {"path": "coldstart_item_appeal_ablation.egg-info", "category": "build",
    "reason": "editable-install metadata, regenerated by `uv pip install -e .`"},
  {"path": "out/payloads", "category": "intermediate_cache",
    "reason": "rank-array pickle cache used by analyze_ablation.py; fully re-derivable from out/method_out/*.json parts (metadata_rank_* per cell) plus a ~8 min `uv run method.py --evaluate --mode full` re-run"}
]}